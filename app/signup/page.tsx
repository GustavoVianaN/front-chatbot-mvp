'use client';
import { FormEvent, useState } from 'react';
export default function SignupPage() {
  const [form,setForm]=useState({name:'',email:'',password:'',companyName:'',acceptTerms:false,acceptPrivacy:false}); const [message,setMessage]=useState(''); const [busy,setBusy]=useState(false);
  async function submit(e:FormEvent){e.preventDefault();setBusy(true);setMessage(''); const r=await fetch('/api/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)}); const p=await r.json().catch(()=>({})); setBusy(false); if(!r.ok){setMessage(p.error||'Não foi possível criar a conta.');return;} setMessage('Conta criada. Você já pode entrar.');}
  return <main className="flex min-h-screen items-center justify-center bg-slate-950 p-6 text-white"><form onSubmit={submit} className="w-full max-w-lg rounded-3xl border border-slate-800 bg-slate-900 p-8"><p className="text-xs uppercase tracking-[.25em] text-emerald-300">14 dias para testar</p><h1 className="mt-3 text-3xl font-semibold">Crie sua conta</h1><div className="mt-7 grid gap-4">{[['name','Seu nome','text'],['email','E-mail','email'],['companyName','Nome da empresa','text'],['password','Senha (mínimo 10 caracteres)','password']].map(([key,label,type])=><label key={key} className="text-sm text-slate-300">{label}<input required type={type} value={form[key as keyof typeof form] as string} onChange={e=>setForm(v=>({...v,[key]:e.target.value}))} className="mt-2 w-full rounded-2xl border border-slate-700 bg-slate-950 px-4 py-3 text-white"/></label>)}</div><label className="mt-5 flex gap-3 text-sm text-slate-300"><input required type="checkbox" checked={form.acceptTerms} onChange={e=>setForm(v=>({...v,acceptTerms:e.target.checked}))}/>Aceito os <a className="text-emerald-300" href="/legal/terms">Termos de Uso</a>.</label><label className="mt-3 flex gap-3 text-sm text-slate-300"><input required type="checkbox" checked={form.acceptPrivacy} onChange={e=>setForm(v=>({...v,acceptPrivacy:e.target.checked}))}/>Li a <a className="text-emerald-300" href="/legal/privacy">Política de Privacidade</a>.</label>{message&&<p className="mt-4 rounded-xl bg-slate-950 p-3 text-sm">{message}</p>}<button disabled={busy} className="mt-6 w-full rounded-2xl bg-emerald-600 px-4 py-3 font-semibold disabled:opacity-60">{busy?'Criando...':'Criar conta'}</button><a href="/login" className="mt-4 block text-center text-sm text-slate-400">Já tenho conta</a></form></main>;
}
