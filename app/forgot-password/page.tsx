'use client';
import { FormEvent, useState } from 'react';
export default function ForgotPasswordPage(){const[email,setEmail]=useState('');const[msg,setMsg]=useState('');async function submit(e:FormEvent){e.preventDefault();await fetch('/api/auth/forgot-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});setMsg('Se o e-mail existir, você receberá as instruções de recuperação.');}return <main className="flex min-h-screen items-center justify-center bg-slate-950 p-6 text-white"><form onSubmit={submit} className="w-full max-w-md rounded-3xl border border-slate-800 bg-slate-900 p-8"><h1 className="text-3xl font-semibold">Recuperar senha</h1><p className="mt-3 text-sm text-slate-400">Informe o e-mail cadastrado.</p><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-6 w-full rounded-2xl border border-slate-700 bg-slate-950 px-4 py-3"/>{msg&&<p className="mt-4 text-sm text-emerald-200">{msg}</p>}<button className="mt-5 w-full rounded-2xl bg-emerald-600 px-4 py-3 font-semibold">Enviar instruções</button></form></main>}
